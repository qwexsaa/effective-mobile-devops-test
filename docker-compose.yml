services:
  backend:
    build: ./backend
    container_name: hello-app
    expose:
      - "8080"
  nginx:
    image: nginx:alpine  
    container_name: hello-nginx
    ports:
      - "80:80"  
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro 
    depends_on:
      - backend  

